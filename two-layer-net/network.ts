class Network {
    public layers: Layer[]
    public weights: Matrix // generate weights for each neuron in each layer

    // loop all layers
     // 1. Get all input from the nodes and construct a matrix
     // 2. Get all weights
     // 3. multiply matrices
     // 4. Generate output by running each matrix element through a sigmoid function
}
